<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/view_span"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/item_file_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:background="@drawable/item_file_round_corner"
        android:src="@drawable/wb_icon_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/item_file_description"
        android:layout_width="45dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:ellipsize="end"
        android:maxLines="2"
        android:layout_marginTop="5dp"
        android:text="test"
        android:textColor="@color/black"
        android:textSize="@dimen/file_selector_item_size"
        app:layout_constraintEnd_toEndOf="@+id/item_file_icon"
        app:layout_constraintStart_toStartOf="@+id/item_file_icon"
        app:layout_constraintTop_toBottomOf="@+id/item_file_icon"/>

</androidx.constraintlayout.widget.ConstraintLayout>